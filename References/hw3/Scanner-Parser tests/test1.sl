assert ROW : "quote with \t and \n and \s and \" and \\ "

def TABLE[(2 < 3)][x]{
	5 ;
	x *= !y;
	for   ( z= t..y){
		if(TABLE[a][b]){
			s = 1 \/ x
		}
	};
	if (ROWS) {
		func(a, b, c);
		(1)
	}
	else{
		1;
		5 /\ foo()
	};
	y /= 7
}
assert TABLE[i][j] : "something" //comment
var _v = COLS
var r = TABLE[x][d]
/* co
mment */

def goo(){
	f > 7; /*co
	mment */
	ROWS;
	// comment
	for (i = 1..4){
		 for (j = 1..9){
		 	if (1){
		 		call()
		 	}
		 	else{
		 	};
		 	if (true){
		 	}
		 };
		 for (j = 1..t){
		 }
	};
	l += a > !c < d \/ foo(a > b) * COLS
}

def func(){}
def func(a){}
def TABLE[x][i]{}
def table[table[4][5]][u]{}